[package]
name = "sats-badge-factory"
version = "0.1.0"
edition = "2021"

[dependencies]
scrypto = { version = "1.3.0" }

[lib]
crate-type = ["cdylib", "lib"]

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = "abort"
strip = true
